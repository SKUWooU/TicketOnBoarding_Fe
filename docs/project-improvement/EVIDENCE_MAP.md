# 개선 근거 연결표

| 문제                                                              | 확인 근거                                                                   | 후속 개선                                                                               | 검증                                                                                             | Issue/PR        |
| ----------------------------------------------------------------- | --------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | --------------- |
| 공용 컴포넌트의 props 계약이 선언되지 않아 `react/prop-types` 오류가 누적됨 | ESLint 기준선 95 errors·2 warnings 중 `react/prop-types` 61건, 카드·리뷰·입력·인증 Provider 13개 컴포넌트 | 실제 사용 props의 문자열·숫자·객체·콜백·children 타입을 `PropTypes`로 선언 | `react/prop-types` 61→0, 전체 errors 95→34; 72 tests·production build 통과 | 성능·운영 품질 결과가 아닌 정적 계약 기준선이며, 남은 unused/문자열/Hook 규칙은 별도 판단 필요 | [FE #37](https://github.com/SKUWooU/TicketOnBoarding_Fe/issues/37) / [상세 근거](component-prop-contracts.md) |
| 모든 page module이 eager import되어 홈 화면에도 관리자·계정·결제·예매 코드가 초기 entry에 포함됨 | Vite build `index-*.js` 577.00 kB / gzip 187.78 kB, `App.jsx` 31개 page 정적 import | page별 `React.lazy`와 `Suspense` fallback으로 route code 전달 시점 분리, mock Browser E2E timezone을 fixture와 같은 `Asia/Seoul`로 고정 | build artifact: entry 223.10 kB / gzip 74.97 kB, Main 68.57 kB / gzip 18.64 kB, ConcertReservation 184.53 kB / gzip 63.39 kB; 72 tests·mock Browser 3 viewport·local Browser E2E | 초기 entry artifact는 감소했고 mock Browser 날짜 계약을 안정화했다. 실제 네트워크·Core Web Vitals·운영 성능 결과 아님 | [FE #35](https://github.com/SKUWooU/TicketOnBoarding_Fe/issues/35) / [PR #36](https://github.com/SKUWooU/TicketOnBoarding_Fe/pull/36) / [상세 근거](route-initial-bundle-split.md) |
| local Browser E2E가 fixture JWT를 cookie에 주입하면서 `/auth/valid`만 route mock 처리함 | Backend token endpoint는 JWT만 만들고 `AuthController`는 `SiteUser`를 조회하던 기준선 | Backend loadtest 사용자 fixture를 소비하도록 route mock 제거 | loopback Backend·MariaDB fixture·Chromium AuthContext 응답·hold/Checkout 취소 snapshot | Chromium에서 `valid: true`·fixture username 확인 뒤 hold·Checkout 취소·2,000석 snapshot 불변식 복구. local fixture 결과이며 OAuth·PG·KOPIS 호출/운영 E2E가 아님 | [FE #33](https://github.com/SKUWooU/TicketOnBoarding_Fe/issues/33) / [PR #34](https://github.com/SKUWooU/TicketOnBoarding_Fe/pull/34) |
| mock Browser E2E와 local FE·BE 통합 테스트가 분리되어 실제 UI의 hold·Checkout 취소 계약을 함께 확인할 수 없음 | FE #27은 mock API, FE #29는 jsdom local integration으로 브라우저↔Backend 경계가 분리됨 | loopback Chromium E2E에서 인증 cookie·가상 2,000석 fixture·hold·Checkout 취소와 snapshot을 연결 | opt-in local Backend Browser E2E 1건, 외부 PG·KOPIS·Kakao 차단 | FE #31 / PR #32 |
| 결제 화면에서 좌석 선택으로 돌아가도 `READY` Checkout·활성 좌석 귀속이 남음 | 기존 `Payment` 복귀는 화면 이동만 수행, Backend에는 멱등 Checkout 취소 API 존재 | `CANCELED` 확인 뒤에만 session 제거·좌석 선택 복귀, 불명확 결과는 결제 화면 유지 | API client·Payment fixture와 local Backend/MariaDB 2,000석 run: 취소 뒤 hold·예약·결제 row 0 | FE #29 / PR #30 |
| jsdom·HTTP 통합 테스트만으로 실제 Chromium CSS·스크롤·클릭 흐름을 확인할 수 없음 | 390px mobile에서 고정 행 상세 영역이 달력 클릭을 가로챈 browser 재현 | Playwright Chromium 3 viewport E2E와 640px 이하 상세 영역 세로·유동 폭 전환 | mock API fixture·desktop/tablet/mobile 3건, hold 성공·409 피드백, production build·변경 JS lint | 10구역·선택 구역 200석·내부 가로 탐색, hold 성공 뒤 소유 좌석 표시, 409 후 선택 미생성; mobile 달력 클릭 회복. 외부 API·운영 성능 주장이 아닌 local browser fixture | FE #27 / PR 대기 |
| FE에 협업·검증 기준선이 없음                                      | `.github`, test script, workflow 부재                                       | Backend 동일 template과 Vitest·CI 구성                                                  | install·build·test 및 legacy lint 실행                                                           | FE #2 / PR #3   |
| 좌석 조회가 임시 점유와 확정 예약을 같은 상태로 표시              | `ConcertReservation.jsx`가 `reserved` boolean만 사용                        | enum 정규화·상태별 버튼·만료 안내·회차 전환 초기화                                      | API fixture, 선택 차단·legacy fallback·stale 응답 test                                           | FE #8 / PR #9   |
| 좌석 선택이 서버 점유와 연결되지 않음                             | `ConcertReservation.jsx`의 24석·로컬 `selectedSeats`                        | 서버 점유·해제, 소유 좌석별 TTL·최소 countdown 연동                                     | API fixture와 전체 좌석 POST, DELETE, 400·401·409·TTL·결제 이동 test                             | FE #10 / PR #11 |
| 결제 callback이 서버 검증 없이 legacy 예약을 직접 호출            | `Payment.jsx`, 삭제 전 `PaymentInosis.jsx`                                  | 서버 Checkout·검증 예약·멱등 키·상태별 재결제 차단으로 전환                             | 53 tests: 서버 금액, 이중 클릭, 새로고침, 409·410·422·503, 검증 재시도, UNKNOWN, 외부 PG 미호출  | FE #12 / PR #13 |
| FE의 Checkout 성공 흐름이 mock HTTP에서만 검증됨                  | FE·BE 저장소 사이 실제 endpoint·cookie·멱등 키·응답 계약 미검증             | 기존 Backend `local,loadtest`와 실제 FE API 모듈·Payment 화면의 opt-in 통합 계약 테스트 | 독립 2회: 2,000석 중 1석, 잔여 1,999, Reservation·Booking·Payment 각 1, 반복 검증 후 불변식 충족 | FE #15 / PR #16 |
| FE·BE가 24석을 각각 하드코딩하고 2,000석 fixture의 UI 계약이 없음 | 현재 매퍼에 2,000석 응답 주입 시 API 좌석 2,000개 미사용·24석 `UNAVAILABLE` | 서버 소유 layout metadata·구역 요약·선택 구역 상세 계약을 후속 Issue로 분리             | jsdom 3회 중앙: 24→2,000석 initial 20.36→492.98ms, 상태 rerender 포함 23.03→880.06ms             | FE #18 / PR #19 |
| Backend 구역 계약이 있어도 FE가 24석 고정 layout만 표시          | `seat-sections` 로컬 응답: 10구역·총 2,000석, S01 상세 200석               | 구역 요약→선택 구역 상세 조회, 서버 행 순서 렌더링, 409에만 legacy fallback             | 로컬 `local,loadtest` 실응답 2,000/200, Vitest 67개·build·변경 lint 통과                          | FE #21 / PR #22 |
| loadtest 공연의 실제 상세→회차→구역→점유 화면 회귀가 없음         | 기존 화면 테스트는 Axios mock, Checkout 통합 테스트는 예매 페이지 미렌더링 | 실제 `local,loadtest` Backend를 소비하는 예매 화면 통합 테스트                          | 가상 공연·회차 1개, 전체 2,000석, 구역 10개, 선택 구역 200석, 실제 hold 후 선택 상태            | FE #24 / PR #25 |
| 운영 의존성에 critical·high 경로가 존재                           | 2026-08-31 `npm audit --omit=dev`: 11건                                     | 현재 major 내 direct·transitive 호환 upgrade                                            | 운영 audit 2 moderate·network 없는 호환성 test·build·CI                                          | FE #6 / PR #7   |
| 중단한 Vercel 자동 배포와 종료된 Backend proxy 설정이 남음        | PR #3 Preview check, `vercel.json`, `vercel.config`                         | Dashboard Git disconnect와 저장소 설정 제거                                             | PR #5 Vercel check 부재·local test·build·CI                                                      | FE #4 / PR #5   |
